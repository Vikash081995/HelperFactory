//use slice to get a substring from a string
let str = "hello world";
console.log(str.slice(0, 5)); //hello


str.slice(0,5) //returns the substring from index 0 to 5 (5 not included)


str.slice(5);
//returns the substring from index 5 to the end of the string
//output: " world"

